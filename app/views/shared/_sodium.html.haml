- if Settings.client_encrypted_only || gallery.client_encrypted
  -# Sodium is now imported directly via ESM in the encrypted_image and
  -# client_encryption_key Stimulus controllers. No separate pack needed.
