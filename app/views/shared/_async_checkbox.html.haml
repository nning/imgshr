.async-checkbox{data: {controller: 'async-checkbox', 'async-checkbox-reload-value': reload}}
  = check_box_tag name, '1', checked,
    id: name,
    data: {action: 'change->async-checkbox#toggle', 'async-checkbox-uri-value': uri}
  - if label
    = label_tag name, label
