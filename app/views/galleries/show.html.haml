= render partial: 'header'

#pictures{data: {endless: @gallery.endless_page}}
  #picture_grid
    = cache [@gallery, :show, :page, params[:page]] do
      - @picture_groups.each do |group|
        .row= render group

  = render 'pagination'
