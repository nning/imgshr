%h2 Galleries

%table.table.table-striped
  %tr
    %th #
    %th Slug
    %th Name
    %th Number of images
    %th
  - @galleries.each do |gallery|
    %tr
      %td= gallery.id
      %td= best_in_place gallery, :slug
      %td= best_in_place gallery, :name
      %td= gallery.pictures.count
      %td
        = link_to gallery_path(gallery.slug) do
          = icon :eye_open
        = link_to gallery_path(gallery), method: :delete, data: {confirm: "Really delete #{gallery}?"} do
          = icon :trash
