= link_to :back, class: 'back', title: picture.gallery, data: { toggle: 'tooltip', placement: 'right' } do
  - if gallery_referer?(picture)
    = icon :circle_arrow_left
  - else
    = icon :th_large
