version: '3'

services:
  # mariadb:
  #   image: mariadb
  #   environment:
  #     - MYSQL_ALLOW_EMPTY_PASSWORD=1
  #   ports:
  #     - 3306:3306
  postgres:
    image: postgres:12
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DATABASE: imgshr_development
    shm_size: '256M'
  redis:
    image: redis:alpine
    ports:
      - 6379:6379
